{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "title": "Pagetype explanationPic content",
    "description": "Description of the content of the Pagetype 'explanationPic'",
    "type": "object",
    "properties": {
        "text":{
            "description": "The text shown on the Page",
            "type": "string"
        },
        "eeg":{
            "description":"Will send a trigger to the eeg if set to 'trigger'",
            "type": "string",
            "enum": ["trigger"]
        },
        "img":{
            "description":"Filename of the image. Has to have the file ending included. Possible endings are 'mp4', 'jpg', 'jpeg', 'png'",
            "type": "string"
        },
        "imagePosition":{
            "description":"Sets the position of the image on the Page",
            "type": "string",
            "enum": ["top", "left", "right", "bottom", "fullscreen"]
        },
        "light":{
            "description":"Switches the light on",
            "type": "string",
            "enum": ["on"]
        },
        "continueDelay":{
            "description": "The amount of seconds after which the Continue Button is shown",
            "type": "number"
        }
    },
    "required": [
        "text",
        "img",
        "imagePosition"
    ]
}