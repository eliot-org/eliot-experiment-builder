{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "title": "Questiontype input options",
    "description": "Description of the options of the Questiontype 'input '",
    "type": "object",
    "properties": {
        "type":{
            "description":"What the user can input. Either 'text' for text or 'num' for numbers",
            "type":"string",
            "enum": ["text", "num"]
        }
    },
    "required": ["type"]
}